var $projectsTable = $('#projects_table');
var $projectsTableDesc = $('#projects_table_desc');
var $map = $('#map');
var $projectsCount = $('#projects-count');
var $orderFilterInput = $('.order_filter');

$projectsTable.html('<%= j(render partial: "decidim/budgets/votes/projects_list").strip.html_safe %>');
$projectsTableDesc.html('<%= j(render partial: "decidim/budgets/votes/projects_info").strip.html_safe %>');

var $dropdownMenu = $('.dropdown.menu', $projectsTable);
$dropdownMenu.foundation();

window.initializeProjects();
